export const getPathProgress = (isStarted) => (isStarted ? 48 : 8)

export const getPathActionLabel = (isStarted) => (isStarted ? 'Continue Path' : 'Start Path')

export const getNextStepLabel = (isStarted) => (isStarted ? 'Continue with recommended topics' : 'Start now and open recommended topics')
