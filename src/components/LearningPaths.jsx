import { learningPaths } from '../data/learningPaths';
export default function LearningPaths({state,setState,setToast,openModules}){const tabs=['Beginner','Intermediate','Advanced','Certification'];const tab=state.currentTab||tabs[0];const setTab=(t)=>setState(s=>({...s,currentTab:t}));const start=(p)=>{setState(s=>({...s,activePaths:[...new Set([...s.activePaths,p.id])]}));setToast(`${p.name} started`)};return <section id='paths' className='max-w-7xl mx-auto p-4'><div className='flex gap-2'>{tabs.map(t=><button key={t} onClick={()=>setTab(t)}>{t}</button>)}</div><div className='grid md:grid-cols-2 gap-3 mt-3'>{learningPaths.filter(p=>p.tab===tab).map(p=><div key={p.id} className='rounded-2xl p-4 border border-white/10'><h3>{p.name}</h3><p>{p.description}</p><p>{p.modules} modules • {p.duration}</p><button onClick={()=>start(p)}>Start/Continue</button><button onClick={()=>openModules(p)}>View Modules</button></div>)}</div></section>}
