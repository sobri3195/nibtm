import { communityThreads } from '../data/communityThreads';
import { datasets } from '../data/datasets';
export default function Workspace({state,setState,resetLocal,exportData,importData}){return <section id='workspace' className='max-w-7xl mx-auto p-4'><h2>Workspace</h2><p>Portfolio readiness: {state.portfolioScore}%</p><p>active paths {state.activePaths.length} saved topics {state.savedTopics.length} saved datasets {state.savedDatasets.length}</p><div className='my-3'><h3>Community</h3>{communityThreads.map(t=><button key={t.id} onClick={()=>setState(s=>({...s,savedThreads:[...new Set([...s.savedThreads,t.id])]}))}>{t.title}</button>)}</div><div><h3>Research Hub</h3><button onClick={()=>setState(s=>({...s,researchIdeas:[...s.researchIdeas,{id:Date.now(),title:'Idea',dataset:datasets[0].id}]}))}>Save Draft</button></div><div><h3>Admin Mini Panel</h3><p>This is a frontend-only local admin simulation. No server database is connected.</p><button onClick={resetLocal}>Reset local progress</button><button onClick={exportData}>Export JSON</button><button onClick={importData}>Import JSON</button></div></section>}
