import { datasets } from '../data/datasets';
export default function DatasetsSection({state,setState,openDataset}){return <section id='datasets' className='max-w-7xl mx-auto p-4 grid md:grid-cols-2 gap-3'>{datasets.map(d=><div key={d.id} className='rounded-2xl p-4 border'><h4>{d.name}</h4><p>{d.domain} • {d.format} • {d.records}</p><div className='h-2 bg-slate-700 rounded'><div className='h-2 bg-violet-500 rounded' style={{width:`${d.health}%`}}/></div><button onClick={()=>openDataset(d)}>Open Dataset</button><button onClick={()=>setState(s=>({...s,savedDatasets:[...new Set([...s.savedDatasets,d.id])]}))}>{state.savedDatasets.includes(d.id)?'Saved':'Save Dataset'}</button></div>)}</section>}
