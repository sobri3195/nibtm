import { topics } from '../data/topics';
import { useMemo,useState } from 'react';
export default function TopicsExplorer({state,setState}){const [q,setQ]=useState('');const filtered=useMemo(()=>topics.filter(t=>t.title.toLowerCase().includes(q.toLowerCase())),[q]);const save=(id)=>setState(s=>({...s,savedTopics:[...new Set([...s.savedTopics,id])]}));return <section id='topics' className='max-w-7xl mx-auto p-4'><input value={q} onChange={e=>setQ(e.target.value)} placeholder='search topic' className='px-3 py-2 rounded-xl text-black'/><div className='grid md:grid-cols-3 gap-3 mt-3'>{filtered.map(t=><div key={t.id} className='rounded-2xl p-4 border'><h4>{t.title}</h4><p>{t.domain} • {t.level}</p><button onClick={()=>save(t.id)}>{state.savedTopics.includes(t.id)?'Saved':'Save Topic'}</button></div>)}</div>{!filtered.length&&<p>No topics found.</p>}</section>}
